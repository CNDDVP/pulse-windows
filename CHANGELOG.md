# 更新日志 (Changelog)

所有针对 Pulse for Windows 的重要版本更新都将记录在此文件中。

## [0.3.17] - 2026-09-19

### 修复

- **测试连接读数即时生效**：账号"测试连接"获取的读数即刻落缓存并广播到全部窗口——此前测试成功但设置页拿不到额度窗口列表，状态也停留在"尚未查询读数"。
- **保存凭据立即刷新**：保存凭据成功后后台立即发起一次该账号刷新，不再等待最长 5 分钟的轮询周期。
- **保存并测试自动带上新凭据**：在输入框粘贴了新 Key 时点击"保存并测试连接"，会自动先保存凭据再测试，不再报"未发现可用凭据"。
- **悬浮栏不再误显"添加账号"**：悬浮栏改为按已启用的账号配置渲染槽位——读数未到达时显示灰色待机环（带服务商图标，点击即刷新），只有完全没有任何启用账号时才显示"添加账号"。
- **Antigravity 预设额度窗口**：读数未到达时可从预设选项（Gemini / Claude 与 GPT 的 5 小时与每周限额）直接指定主环与时间环；已配置但暂无读数的窗口 ID 保留占位，不被清空。

## [0.3.16] - 2026-09-20

### 新增与功能一致性完善

- **数据目录级单实例隔离**：单实例锁由全局应用标识改为以规范化数据目录绝对路径（Canonical Data Directory）的哈希为作用域（`Pulse_Instance_Mutex_<hash>` 与 `Pulse_Instance_Window_<hash>`）。安装版与便携版、以及放置在不同目录的多个便携版实例可互不干扰、独立并发运行，彻底消除多实例运行冲突。
- **便携版开机自启与路径自愈**：
  - 便携版开机自启采用独立的注册表项（`Pulse_Portable_<profile_id>`），与安装版互不覆盖；
  - 启动时自动执行路径检测与自愈（`repair_startup_if_moved`）：当用户将便携版目录移动或重命名后，首次运行自动更新注册表中的自启路径，消除系统死链；
  - 通用设置页面解除了便携版对开机自启开关的禁用，并补充路径自愈说明。
- **首次使用配置与凭据迁移支持**：
  - 便携版首次运行且无账号时，自动检测本机是否存在已安装版的配置，并在设置页顶部展示友好的一键导入横幅；
  - 提供安全安全的后端导入流程（`import_installed_config`），在 Windows 凭据管理器中自动将旧 Profile 凭据克隆至便携版新 Profile，密码全程不暴露至前端或日志；
  - 关于页中同时提供持久的“从本机安装版导入配置与凭据”入口。
- **关于页运行时诊断与一键复制脱敏**：
  - 完整展示当前运行时信息（版本、Commit、构建时间、部署模式、Profile ID、运行 EXE 的 SHA256 完整哈希、数据目录与程序路径）；
  - 提供“复制脱敏诊断”功能，自动将所有用户本地路径转换为 `[PATH]`，方便用户安全提交 Issue。
- **圆环与时间环下拉项占位保留**：
  - 当账号已保存了 `primary_window` / `secondary_window` / `elapsed_window`，但尚未取得实时读数时，下拉框以 `[已配置 · 等待读数]` 占位保留原设置，杜绝保存其他字段时被意外清空。
- **构建管线发布一致性验证**：
  - 更新 `scripts/build-dist.ps1`，确保 release 构建出的单一 `pulse-windows.exe` 同时用于 NSIS 安装包与便携 ZIP；
  - 增加构建产物 EXE SHA256 校验与对比日志，并在 `BUILD_INFO.txt` 中固化二进制指纹。

## [0.3.15] - 2026-09-19

### 修复

- **设置窗口原生 X / Alt+F4 无响应**：后端的关闭转发事件此前只对 settings 窗口定向发送，前端的全局监听收不到——现在改为全局广播 + 窗口定向双通道，前端监听统一在挂载时绑定一次并用 ref 持有最新回调（消除闭包滞后与频繁重建）。原生关闭、Esc、按钮三条路径行为一致。
- **未获取读数时圆环设置项消失**：全新便携环境（尚无读数）下"主圆环额度 / 外圈时间环 / 第二内环"三个设置项会被条件渲染隐藏。现在无条件展示并配空态提示（连接成功后下拉自动填充），便携版与安装版设置结构一致。

## [0.3.14] - 2026-09-19

### 修复与优化

- **Kimi Code 活动灯深度思考超时与状态机加固**：
  - 会话衰减超时由 3 分钟调整为 30 分钟（`SESSION_DECAY_SECS = 1800`）：解决 K2.8 High 等深度推理模型在单步长思考（5~15 分钟）期间无磁盘写入时小白点提前熄灭的问题；思考期小白点保持持续旋转，直到生成结束。
  - 增加对用户主动打断与失败事件（`turn.failed`、`turn.cancelled`、`turn.interrupted`、`prompt.failed`、`prompt.cancelled`）的捕获，确保异常中断 0 延迟即时熄灭。
  - 接入 `event.session.work_changed`（`busy: true` / `busy: false`）作为双重状态保底。
- **活动指示器追踪日志硬编码修复**：`activity-trace.log` 中记录的来源不再硬编码为 `codex`，而是根据实际活动提供方动态记录真实 `{source}`（如 `kimi`、`claude`、`zhipu`、`antigravity`）。
- **前端 React 渲染纯度优化**：`UsageRing.tsx` 中的旋转相位与刷新彩弧延迟计算改为 `useState(() => ...)` 惰性初始化，消除 5 处 Oxlint 渲染纯度与 Ref 警告，符合 React 19 编译器纯函数规范。
- **托盘右键退出一致性**：托盘右键退出统一为 `app.exit(0)`，与悬浮栏右键退出保持一致，确保生命周期与资源释放正常完成。

## [0.3.13] - 2026-09-19

### 修复

- **Antigravity 拆分模型组的悬浮栏高度裁剪**：槽位数计算统一为 `item_count`——此前启动定位/断屏恢复路径漏算拆分槽位（按账号数而非槽位数），开启 `split_model_groups` 时底部项目被裁剪。
- **About 页阻塞式确认框**改为非阻塞二次点击确认（armed 态 + 3 秒自动解除），与设置页其余破坏性操作一致，杜绝 WebView2 渲染线程挂起风险。

### 其他

- 清理 Rust 编译警告（未用导入），release 构建零警告。

## [0.3.12] - 2026-09-19

### 修复（活动灯信号源基于真实日志事件重写）

- **ZCode（智谱）**：改读 `~/.zcode/cli/log/zcode-*.jsonl` 结构化事件日志——`turn.started` / `model.request.started` / `tool.call.started` 点亮，`turn.completed` / `turn.failed` 熄灯，取代此前"任何文件写入即点亮"的字节级判定。高频噪声事件（hook 运行失败、内存采样、持久化等，每天数千条）被正确忽略，灯的亮灭与真实对话轮次一一对应。
- **Antigravity**：废除按字节增长量的阈值判定（空闲心跳误亮的边界），改为增量中包含 `streamGenerateContent` 请求特征才点亮（`loadCodeAssist` 等空闲心跳不触发）；无明确结束事件，20 秒无请求平滑熄灯（其他来源仍为 3 分钟）。
- 事件清单核对自本机真实日志的全部 80+ 种事件名，测试覆盖点亮/熄灯/生命周期/心跳过滤。

## [0.3.11] - 2026-09-19

### 新增

- **右键/托盘「立即刷新」同步转圈**：为所有发起的账号广播刷新状态，悬浮环同步转起、数据到的先停；手动刷新不再受周期冷却抑制（退避中的除外），每次点击必响应。
- **双击悬浮环触发全量刷新**：单击 250ms 防抖只刷新单个账号，双击取消单刷并全量同步；拖拽手势仍会取消挂起的单击，不误触。
- **活动灯诊断轨迹**：`activity-trace.log` 记录每次状态变化的账号、方向与触发原因（200KB 自动重置），"结束后是否正常熄灯"从此有直接证据。

### 修复（v0.3.10 复审 21 项）

- **旧设置快照覆盖新设置**：设置表单不再提交位置字段（位置的唯一写方是拖拽/窗口路径）；拖拽保存递增配置代际，使在途刷新结果按新位置口径失效。
- **清理全部凭据同步失效**：全部读数清空、在途请求失效、纳入保存串行锁。
- **Profile 写盘失败身份漂移**：凭据键含 profile_id，随机新 ID 会让已存凭据不可达且重启再漂移；改用稳定散列派生的回退身份，反复失败时凭据保持可达。
- **结果提交竞态**：提交前在写锁内复查配置代际，堵住检查与写缓存之间凭据/身份变化的窗口。
- **活动灯结束后误亮**：Codex 的 token_count 用量遥测移出工作信号（它与完成事件同秒出现，扫描切批会把刚熄的灯重新点亮）；ZCode 灯不再监控后台任务索引库（后台写入与对话无关，会把空闲点亮）。
- **活动扫描 torn tail 回归**：只有读满预算仍无换行（真正的超长行）才丢弃推进，分两次写入的正常事件保留等待补全。
- **坏行标记持久化**：连续扫描跳过未变化文件时，历史坏行的不完整标记不再丢失。
- **全新环境打包 Null 错误**：受保护目录不存在时跳过检查。
- **其余**：保存凭据后广播清空旧额度、身份切换写回内存设置、拖动位移改用屏幕坐标、断屏检测用窗口中心点、Command Code 必需请求 8 秒上限、删除账号同步基线镜像、凭据写盘失败回滚、详情窗口执行减少动态、手动超时口径与定时一致、持久缓存写盘串行、凭据来源变化失效在途请求、退避拒绝发反馈事件、顺序调整不带账号草稿、移除点击图标绿色外框。

## [0.3.10] - 2026-09-19

### 修复（外部运行时审计第二批 12 项 + 第三批 3 项）

- **设置保存事务化（A03）**：表单保存、凭据保存/删除、删账号、拖拽保存五条写路径全程持串行锁——并发保存与拖拽交错时不再互相覆盖（拖拽写入的位置此前可能被写盘中的旧快照抹掉）。
- **凭据变更立即清读数（A06）**：保存/删除凭据后广播清空后的读数，悬浮栏不再保留旧额度。
- **手动刷新统一提交语义（A10）**：单账号手动刷新与定时轮相同的通知判断和持久缓存落盘。
- **刷新全部如实反馈（A07）**：返回实际发起/跳过账号数，冷却期全跳过时如实提示而非"已刷新全部"。
- **可选请求预算隔离（A22）**：小米 detail/balance 与 Command Code subscriptions 各 6 秒短超时，装饰性请求不再耗尽总预算丢掉已成功的主额度。
- **Profile 损坏不再静默换身份（A18）**：保留坏文件副本与日志；新身份写盘失败留可诊断记录。
- **重新生成身份事务化（A25）**：清读数与调度、失效全部在途请求。
- **Windsurf 新鲜度（A23）**：元数据不可得也标"未验证"，不把旧额度包装成实时。
- **Token 统计可信（A13/A14/A20/A21）**：来源未安装不算扫描截断；删除裸 "flash"/"pro" 价格兜底并显示未计价覆盖率；表格显式排序；坏行缺口进提示。
- **普通设置保存隔离账号草稿（A26）**：只提交 providers 已保存基线，不打掉正在编辑的草稿。
- **缓存过期独立推进（A19）**：长刷新间隔/退避期间，超期读数由后台定时转为过期并广播，不再依赖下一次请求成功。
- **自由模式断屏恢复（A15）**：悬浮栏落在已消失的显示器上时自动按比例重挂主屏，多屏热插拔不再导致窗口不可达。

## [0.3.9] - 2026-09-19

### 新增

- **活动卫星灯扩展 ZCode 与 Antigravity**：此前只有 Codex（及 Claude）账号能在 AI 工作时亮灯。现在智谱账号监控 ZCode CLI 的任务日志与会话存储（有写入即工作中），Antigravity 监控其 language server 日志（按增长速率区分空闲心跳与真实任务）。沿用首见跳历史与 3 分钟衰减语义。

### 修复

- **活动状态单向闭锁**：任务结束后卫星灯永不熄灭、直到重启（v0.3.6 活动检测按会话改造引入的回归——状态只报"工作中"不报"已结束"）。现在每轮对全部受监控源对称报告，熄灯路径恢复。
- **卫星灯相位统一**：各账号的灯原来按各自挂载时刻起转、相位散乱；现在统一锚定到同一时钟，任意时刻所有灯处于同一角度。
- **设置页开关文字乱码**（三个替换字符）→ "显示悬浮栏"。
- **详情卡侧方断层**：卡片位于悬浮栏左侧时与悬浮栏之间留有 34-50px 透明区、箭头悬空。按停靠方位水平对齐后实测间隙 0px。
- **主环颜色被次窗口绑架**：主额度 0% 时未选中的周期耗尽也会全环变红。已固定主窗口的只看主窗口。
- **拆分模型组详情混杂**：开启 split_model_groups 后滑过 Gemini 圆环弹出的详情卡仍显示整个账号的额度。现在按组过滤并在标题带组名。
- **微拖吞点击**：起拖即写 400ms 点击抑制导致手抖的刷新点击被无声吞掉。抑制改为仅在真实拖动位移足够时生效。
- **设置窗口三处阻塞式确认框**改为非阻塞二次点击确认（armed 态 + 3 秒自动解除），原生 X / Alt+F4 也走同一未保存确认流程。
- **外部运行时审计第一批 10 项**（A02/A04/A05/A09/A11/A12/A16/A17/A24/A27）：定时刷新与手动刷新并发去重、清凭据失败如实报告、打包脚本输出目录保护边界（此前 4 项危险输入被放行）、跨屏拖动显示器记录落盘、旧刷新回调误关新动画、剩余模式条形图语义、主栏隐藏时同步收起详情卡、原生关闭走未保存确认、活动扫描超长行卡死与文件回收、减少动态效果全覆盖。

## [0.3.8] - 2026-09-19

### 修复

- **详情卡与悬浮栏的视觉间隙**：top 停靠时详情卡在固定高窗口里垂直居中，卡片内容越矮、窗口顶部透明区越大（3 行的 DeepSeek 卡间隙可达 200px），看起来"离悬浮栏很远"。现在按停靠方位贴边渲染：正下方贴顶、正上方贴底、侧向居中；bottom 停靠从 free 兜底分支中拆出，侧向小箭头只在卡片位于悬浮栏左右时绘制。
- **拖动转自由模式的残缺中间态**：拖动中光标离开吸附带时窗口已按竖排尺寸 resize，但布局仍按旧停靠渲染，横排内容被裁成"只剩一个图标"的窄条，直到松手才恢复。后端在停靠边变化时实时推送 `drag-side` 预览事件，前端布局同步切换——拖动中即是完整的自由模式样式。
- **拖动中额度详情面板残留**：拖动跟随的毫秒级相对抖动会重新触发图标 hover，把已关闭的详情再次弹出到屏幕上。前端 hover 回调加拖动守卫，`show_detail` 后端闸门在拖动中直接拒绝。
- **跨 DPI 屏启动恢复的双重缩放**：悬浮栏停在低缩放屏（如 100%）时重启，窗口从主屏（如 150%）恢复的途中 `WM_DPICHANGED` 会把已按目标屏算好的尺寸再乘 DPI 比例（72×476 被缩成 48×317），只装得下 4 个账号。放置后检测落地漂移并补设一次，启动恢复与跨屏拖拽统一走该路径。

## [0.3.7] - 2026-09-19

### 修复
- 月度额度窗口恢复周期长度推断：OpenCode Go 等名称含“每月”的额度现在
  能正常显示速度预测文案（此前因缺少周期长度而整体沉默）。

## [0.3.6] - 2026-09-19

### 修复
- 悬浮栏右键菜单：WebView2 吞掉右键事件导致菜单不可用——改为在原生窗口过程
  上挂子类直接捕获 WM_RBUTTONUP，右键即可弹出（设置/立即刷新/显隐/退出）。
- 活动检测按会话（文件）维护状态：多会话并存时单会话空闲不再清掉其他会话的
  工作状态；追加读取增加每文件与每轮预算，超大积压不再阻塞扫描。
- 机器人“刷新中”不再优先于“工作中”（与上游规则一致）。
- 第二额度内环不再与主额度选同一项，且遵守“剩余”显示模式。
- 拆分展示的 Antigravity 账号按上游规则固定占 2 个悬浮栏槽位。
- 小米 Coding Plan 控制台响应缺少/格式错误的 code 字段不再被当作成功。

## [0.3.5] - 2026-09-18

### 修复
- 放宽 HTTP 连接超时 5 秒 → 10 秒：修复网关透明代理链路下 TLS 握手超过
  5 秒导致 OpenCode Go（及其他慢路由服务商）每次刷新都连接超时的问题。

## [0.3.4] - 2026-09-18

### 修复
- 修复自由拖动时悬浮栏可以被拖出屏幕、松手后半挂在屏幕外的问题：
  拖动过程与松手都会把窗口钳制在当前显示器工作区内，保存/恢复口径一致。

## [0.3.3] - 2026-09-18

### 修复

- **设置保存代际误拒**：双写并发（悬浮栏拖拽 vs 设置表单保存）时 `update_settings` 不再整体拒绝旧代际载荷，自动按代际合并后端的停靠位置字段——拖动悬浮栏不再导致"无法保存设置"。
- **网络错误细化**：Provider 请求失败现在标注主机名与失败类别（超时 / 连接失败 / 解码失败），不再笼统显示"网络请求失败"。

## [0.3.2] - 2026-09-18

### 新增功能
- **动画机器人标记 (Animated Bot Mark)**：
  - 悬浮栏圆环中心的供应商徽章可替换为会眨眼的小机器人：8 种性格（沉稳、热切、踏实、好奇、贪睡、顽皮、深沉、自豪）× 18 种形状 × 自定义颜色任意搭配。
  - 机器人情绪与账号真实状态联动：工作中、抓取中（刷新）、额度耗尽、休眠五种心情；眼睛朝向屏幕所在一侧。
  - 全部实例共享一个 ≤30 FPS 的 rAF 时钟并直接改写 DOM 属性，动画不触发 React 重渲染；页面隐藏时自动暂停。
- **CLI 实时活动检测 (Claude Code / Codex)**：
  - 增量读取 `~/.claude/projects` 与 `~/.codex/sessions` 的 JSONL 会话记录，按事件类型分类出"工作中/空闲"，每 5 秒合并进读数、仅状态变化时推送。
  - 圆环活动指示与机器人"工作中"情绪由此驱动，反映真实使用而非刷新时刻；会话内容只在本模块内分类，仅状态标志对外可见。
- **点击圆环即时刷新单个账号**：
  - 点击任一圆环只刷新该账号，连点自动复用在途请求；刷新期间外环变淡并显示旋转弧（至少 650ms），结束拖拽的松开动作不会误触刷新。
- **第二额度内环 (Secondary Quota Ring)**：拥有多个额度窗口的账号可在主环内侧用细环显示第二个所选额度，按阈值变色；默认关闭，完全显式选择、无自动残留。
- **Antigravity 按模型组拆分展示**：开启后 Gemini 与 Claude/GPT 各占一个悬浮栏槽位；点击任一槽位仍刷新同一账号。
- **小米 Coding Plan 供应商支持**：套餐额度多窗口展示、套餐名称、人民币余额；套餐过期判定为不可用并提示显示的是上周期用量，未购套餐明确提示按量付费账户。
- **减少动态效果设置**：新增全局开关，机器人等连续动画改为静态表情；系统开启"减少动态"偏好时同样自动生效。

### 修复与加固
- **统一拖拽体验（停靠 / 自由模式通用）**：
  - 全新指针状态机：按下即武装、位移超过 6 DIP 才进入拖拽，任何模式下都能直接拖动悬浮栏。
  - 跟随真实系统光标位置，不受窗口自身移动与多显示器 DPI 换算影响；边缘吸附带 32/48 DIP 迟滞，临界抖动不再反复跳边。
  - 尺寸与位置经单次 `SetWindowPos` 原子提交，消除分离调用导致的窗口尺寸震荡；拖拽期间周期性摆放完全让位。
- **右键原生菜单**：右键悬浮栏或收起后的边缘条唤起与托盘一致的原生菜单（原为直接打开设置）；双击边缘条展开悬浮栏。
- **额度预测分级着色**：预测按严重程度（已用尽 / 即将用尽 / 有用尽风险 / 正常）分别以红、橙、黄、绿着色，颜色与文案出自同一判定来源。
- **刷新调度加固**：并发刷新以信号量限流（4 并发）；失败自动按指数退避重试（30s × 2ⁿ，上限 30 分钟），成功后恢复正常间隔。

## [0.3.1] - 2026-09-18

### 新增功能
- **便携模式支持 (Portable Mode)**：
  - 支持通过在可执行文件同目录下放置 `portable.flag` 触发便携运行。
  - 所有设置、统计缓存与 WebView2 用户数据自动保存在同目录 `data/` 下。
  - 引入动态 `profile_id` 机制：同一 Windows 用户移动便携文件夹后凭据自动保持有效。
  - 复制到其他机器或切换系统用户时安全判定为“需要配置”，杜绝凭据泄漏。
- **开机自启安全保护**：在便携模式下自动禁用开机自启注册表写入，防止移动文件夹后产生死链。
- **Token 消耗公有云费用估算**：内置 Claude、GPT-4o、o1/o3、DeepSeek、Gemini 等主流模型公开定价，自动测算历史 Token 预估金额。
- **双包发布支持**：官方同时提供 Windows 11 x64 NSIS 安装程序与解压即用便携版 ZIP。

### 修复与加固
- **悬浮栏与交互**：
  - 彻底修复自由浮动模式下拖动后被定时器拽回屏幕中心的缺陷。
  - 修复停靠模式下悬停展开时整窗擦除闪烁的问题（锁定 72px 轨道，接入独立预热详情覆盖窗）。
  - 修复自由模式下部分场景卡片未显示的问题。
- **并发与配置**：
  - 引入设置 `generation` 版本号与代际守卫，杜绝并发覆写与旧请求迟到覆盖。
  - 配置备份增加微秒时间戳与 UUID 防碰撞，备份失败立即阻断覆写。
- **数据解析与统计**：
  - Codex 单调递减重置保护，解决历史最大计数残留。
  - Gemini 日志有界流式解析，不再粗暴依赖首 1KB 前缀。
  - Devin 本地数据库时效性校验，失效记录标注 `_unverified`。
  - SQLite Token Spend 建立 `(source, ts)` 复合索引提升查询性能。
- **系统安全与隐私**：
  - 细分 Tauri 窗口 Capabilities，读数悬浮窗保持严格最小只读权限。
  - 诊断日志全面脱敏，屏蔽驱动器绝对路径与个人用户目录。
