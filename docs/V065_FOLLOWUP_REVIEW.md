# v0.6.5 续期故障与设计复查

日期：2026-09-22。源码 652035b，检查开始时工作区干净。PID 31136 路径为本仓库 target/release/pulse-windows.exe，文件版本 0.6.5；未核验该 EXE 与提交的密码学对应关系，本轮未再次核验 GitHub 产物。

## 当前实际故障

用户截图显示套餐和用量明细均 HTTP 401，自动续期冷却中，现金余额仍成功。可确定网页来源未恢复认证；不能仅凭截图区分过期、被撤销或会话设备校验失败。冷却文字只证明最近五分钟记录过尝试，不证明续期成功。

## 发现与建议

|编号|级别|代码证据及触发条件|修复方向与验收|
|---|---|---|---|
|S1|P1|stepfun_session.rs:191-242 只打开隐藏首页并轮询 Cookie，没有显式刷新操作，也没有将手工保存的会话导入浏览器；纯手填凭据不保证拥有可续期的浏览器会话。|区分手填 Token 和可续期网页登录会话；确认官网实际刷新协议/触发路径，状态明确显示是否支持自动续期。实际跨过 access token 过期时间验证恢复，不以单次登录成功验收。|
|S2|P1|stepfun_session.rs:289-334 网页登录直接接受浏览器已有有效 Token，未要求选择/确认身份；登录和续期均未按账号隔离 WebView 数据，lib.rs:631 还设置全局用户目录。第二个账号可能复用第一个账号的会话。|按账号隔离会话并核对全局目录覆盖行为，保存前显示待绑定身份并确认。用两个不同账号验收，自动续期继续保留身份校验。|
|S3|P2|stepfun_session.rs:208-221、299-315 在服务验证前记录 checked；相同 Token 一次遇到网络错误后，在本轮等待中永不重试。|按错误分类，对临时网络/限流问题进行有限重试；身份错误不放宽校验。验证同一 Token 首次超时随后成功可以恢复。|
|S4|P2|stepfun_session.rs:174-185 在窗口创建和验证前记录五分钟冷却；后续只有冷却提示，无法保留此前失败原因。|保留脱敏的最近失败阶段、时间与下一次尝试时间；区分窗口创建失败、无会话、Cookie 未变化、身份不符、网络失败和服务拒绝。不得记录原始 Cookie/Token。|
|S5|P2|stepfun_session.rs:207-242 十二秒循环内部还可等待 Cookie 两秒、网络八秒，截止时间不是整个操作的硬上限；恢复后还会额外查询。|使用全流程 deadline/取消清理，剩余预算约束子操作；验证慢网络、更新退出时没有遗留隐藏窗口。|
|S6|P2|providers/stepfun.rs:99-102、118-119 固定第一页 200 条，不按 total 续页；服务若返回不足 200 条但 total 更大，连截断警告都可能缺失。|按服务分页合同分页，限定最大页数/时间预算并标注完整性；用多页及服务缩小页大小的样本验收。|
|S7|P2|providers/stepfun.rs:95-103 status 成功但 records 类型错误/缺失时静默忽略，不报告明细解析失败。|区分合法零记录和 schema 不兼容，保留其他成功数据；以 records 错误类型与合法空数组验收。|
|S8|P2|providers/mod.rs:49-55 将环境变量代理 URL 原样返回前端，GeneralPage.tsx:251 直接显示 detail；若代理含 user:password，会显示密码。|输出代理信息前移除 userinfo 和敏感查询参数，实际连接配置不变；以含用户名密码的合成代理 URL 验证显示及诊断都脱敏。|

## 范围与边界

本轮重新运行：Rust `cargo test --lib` 123 项通过；前端 `npm test` 13 套件、95 项通过。现有离线测试未覆盖真实网页会话跨过期续期，所以通过不代表 S1 已解决。

本轮为复查，未改应用代码、未替换运行中的 EXE。不是全仓库无遗漏保证。重点检查 StepFun 会话、用量请求、代理展示，并查看更新交接锁链；未做 Windows 窗口交互、24 小时驻留、多屏、安装/便携升级回滚验收。v0.6.5 已包含前次 400 参数修正，本次 401 应单独处理，不能继续归因为 startTime 参数。

优先顺序：S1/S2 → S3/S4/S5 → S6/S7/S8。不要通过清除凭据、取消身份校验、无限重试或隐藏警告来掩盖故障。
