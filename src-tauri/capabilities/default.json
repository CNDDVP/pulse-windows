{
  "$schema":"../gen/schemas/desktop-schema.json",
  "identifier":"default",
  "description":"Local application event transport only; secrets remain in Rust",
  "windows":["main","detail","settings"],
  "permissions":[
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
